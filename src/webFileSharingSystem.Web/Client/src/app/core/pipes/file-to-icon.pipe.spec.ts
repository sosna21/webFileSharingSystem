import { FileToIconPipe } from './file-to-icon.pipe';

describe('FileToIconPipe', () => {
  it('create an instance', () => {
    const pipe = new FileToIconPipe();
    expect(pipe).toBeTruthy();
  });
});
